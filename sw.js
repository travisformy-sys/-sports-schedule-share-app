self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?event.data.text():'Team update'}}const title=data.title||'CCA Cardinals';const options={body:data.body||'New team update',icon:'/cardinal-logo.png',badge:'/cardinal-logo.png',tag:data.tag||'cca-team-update',renotify:true,data:{url:data.url||'/?team=cardinal-charter-academy-5c6ec6#messages'}};event.waitUntil(self.registration.showNotification(title,options))});self.addEventListener('notificationclick',event=>{event.notification.close();const target=event.notification.data?.url||'/?team=cardinal-charter-academy-5c6ec6#messages';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate(target);return c.focus()}}return clients.openWindow(target)}))});