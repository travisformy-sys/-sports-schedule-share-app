'use client';
import {FormEvent,useState} from 'react';
import {createBrowserSupabase} from '../../lib/supabase/client';
export default function AuthPage(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');try{const s=createBrowserSupabase();const{error}=await s.auth.signInWithOtp({email,options:{emailRedirectTo:`${window.location.origin}/dashboard`}});if(error)throw error;setMsg('Check your email for your secure HomeKeep sign-in link.')}catch(err){setMsg(err instanceof Error?err.message:'Unable to send sign-in link.')}finally{setBusy(false)}}return <main className="shell" style={{maxWidth:560}}><div className="brand">HomeKeep</div><section className="card" style={{marginTop:24}}><h1>Welcome home</h1><p className="muted">Enter your email and we’ll send you a secure sign-in link.</p><form onSubmit={submit} className="grid"><label className="field">Email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><button className="btn primary" disabled={busy}>{busy?'Sending…':'Email me a sign-in link'}</button>{msg&&<p className="muted">{msg}</p>}</form></section></main>}